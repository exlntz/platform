# 🏆 Платформа для подготовки к олимпиадам

Современная платформа для обучения, решения задач с ИИ, соревнований (PvP) и отслеживания прогресса. Проект построен на стеке **FastAPI** (Backend) и **Vue 3** (Frontend).

---

## ✨ Основные возможности

* 🤖 **Генерация задач:** Умное создание уникальных задач с помощью ИИ (Groq API).
* ⚔️ **PvP Режим:** Соревнования между пользователями в реальном времени с системой рейтинга (ELO).
* 📊 **Аналитика:** Подробная статистика успеваемости, графики рейтинга и история решений.
* 🏆 **Лидерборд:** Глобальный рейтинг пользователей.
* 🛡️ **Админ-панель:** Управление пользователями, задачами, логами и статистикой системы.

---

## 🚀 Быстрый старт (Docker)

Самый простой способ запустить проект — использовать Docker Compose.

### 1. Клонирование репозитория

```bash
git clone https://github.com/exlntz/platform.git
cd platform

```

### 2. Настройка окружения

Создайте файл `.env` на основе примера.

```bash
cp .env.example .env

```

> **Важно:** Отредактируйте `.env` и впишите свои данные (API ключи Groq, секретные ключи).

### 3. Запуск контейнеров

Соберите и запустите проект в фоновом режиме:

```bash
docker-compose up -d --build

```

После успешного запуска проект будет доступен по адресам:

* **Frontend (Сайт):** `http://localhost:5173`
* **Backend (API):** `http://localhost:8000`
* **Документация API:** `http://localhost:8000/docs`

---


## 🔑 Доступ к Админ-панели

При первом запуске приложения автоматически создается **Суперпользователь** (Администратор) с данными из вашего `.env` файла.

### Как зайти:

1. Откройте сайт и перейдите на страницу входа: `/auth`.
2. Используйте данные суперпользователя (по умолчанию из `.env`):
* **Username:** `superuser` (или то, что указано в `SUPERUSER`)
* **Пароль:** `superuser` (или то, что указано в `SUPERUSER_PASSWORD`)


3. После входа в шапке сайта появится кнопка **"Админ панель"**.
4. Прямая ссылка: `http://localhost:5173/admin`

---
